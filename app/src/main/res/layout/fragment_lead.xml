<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:id="@+id/fragment_lead"
    android:background="@mipmap/beijing"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/lead_login"
        android:layout_above="@+id/lead_register"
        android:layout_marginBottom="13dp"
        android:background="@drawable/textview_normal_btn"
        android:layout_marginLeft="60dp"
        android:layout_marginRight="60dp"
        android:text="@string/lead_login"
        android:textColor="@color/colorWhite"
        style="@style/normal_tv_btn"/>

    <TextView
        android:id="@+id/lead_register"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="80dp"
        android:layout_marginLeft="60dp"
        android:layout_marginRight="60dp"
        android:background="@drawable/textview_stroke_btn"
        android:text="@string/lead_register"
        android:textColor="@color/colorButton"
        style="@style/normal_tv_btn"/>
</RelativeLayout>